import 'package:flutter/material.dart';

class TextSMSAdminToStaff extends StatelessWidget {
  const TextSMSAdminToStaff({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
